server:
  port: 8765

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true

spring:
  application:
    name: User Login Service
    
zuul:
  routes:
    horror:
      path: /user/**
      service-id: userservice
      strip-prefix: false